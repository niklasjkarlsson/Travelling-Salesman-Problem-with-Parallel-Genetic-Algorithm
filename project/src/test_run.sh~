$N=3
$P=4
$args="../run/TSP_data50.dat 100 100 1 10"
rm -f avgs.dat

for p in {1..$P}
	 sum=0
	 for i in {1..$N}
		  time=$(mpirun -np $p g $args | awk 'END{print}')
		  sum=$sum + $(echo "$time" | awk '{print $NF}')
         done
	 sum / $N >> avgs.dat
done
		  
